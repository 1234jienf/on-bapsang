import 'package:flutter/material.dart';

class ShoppingDetailReviewScreen extends StatelessWidget {
  const ShoppingDetailReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
