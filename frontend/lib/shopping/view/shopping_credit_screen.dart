import 'package:flutter/material.dart';

class ShoppingCreditScreen extends StatelessWidget {
  const ShoppingCreditScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
