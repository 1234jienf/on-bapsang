import 'package:flutter/material.dart';

class RecommandRecipeCard extends StatelessWidget {
  const RecommandRecipeCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
